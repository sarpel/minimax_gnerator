# Edge TTS HTTP API Server Dependencies
# ======================================
# These are the Python packages needed to run the Edge TTS API server.

# Web framework for the REST API
# FastAPI is modern, fast, and generates automatic API documentation
fastapi>=0.100.0

# ASGI server to run FastAPI
# uvicorn is the recommended server for FastAPI applications
uvicorn[standard]>=0.23.0

# Microsoft Edge TTS library
# This is the core library that communicates with Microsoft's TTS service
edge-tts>=6.1.0

# Request validation
# Pydantic provides data validation using Python type annotations
pydantic>=2.0.0
