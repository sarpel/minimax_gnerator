# Bark TTS HTTP API Server Dependencies
# ======================================
# These are the Python packages needed to run the Bark TTS API server.

# Web framework for the REST API
fastapi>=0.100.0

# ASGI server to run FastAPI
uvicorn[standard]>=0.23.0

# Request validation
pydantic>=2.0.0

# Bark TTS from Suno
# CONCEPT: This is the core library that performs text-to-speech synthesis
# It will download models (~5GB) on first use
git+https://github.com/suno-ai/bark.git

# Audio processing
scipy>=1.10.0
numpy>=1.24.0

# PyTorch (CPU version by default - GPU version in docker-compose.gpu.yml)
# SYNTAX: torch is installed via bark's dependencies
